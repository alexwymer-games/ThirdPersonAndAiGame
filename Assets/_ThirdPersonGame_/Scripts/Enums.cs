using UnityEngine;

public enum MovementState
{
    IDLE = 0,
    WALK = 1,
    SPRINT = 2,
    CROUCH = 3,
    JUMP = 4
}


public enum DoorState
{
    NONE,
    OPEN,
    CLOSED
}

public enum DoorStatus
{ 
    NONE, 
    LOCKED,
    UNLOCKED
}


